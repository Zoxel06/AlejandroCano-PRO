public class TeoriaArray {

    // ARRAY --> CONJUNTO FINITO DE DATOS, NO DINÁMICOS

    int[] numero1 = new int[6];
    // se guarda un "0" en cada una de las 6 posiciones (1,2,3,4,5)

    boolean[] acierto = new boolean[4];
    // se guarda un false en cada una de las posiciones (0,1,2,3)

    String[] palabras = new String[2];
    // se guarda un "null" en cada una de las posiciones (0,1)

    int[] numeros = {7,9,20,10,40};
    //se guarda cada numero en cada posicion correspondiente (0 -- 7 / 1 -- 9 / 2 -- 20 / 3 -- 10 / 4 -- 40)
}
